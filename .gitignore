.DS_Store
parts/
py3x/
*.pyc

bin/sunray-srvr
sunray-srvr-debug.log

# ikb
.ikb/buildit.json
.ikb/buildit.jsonc
.ikb/buildit.yml
.ikb/buildit.yaml
.ikb/final.buildit.json
.ikb/plugins
.ikb/buildit-requirements.pip

pip_src/
# customer addons
customer_addons/

.vscode/

etc/

# Traefik
#acme.json
#traefik/

# Docker

# DevContainers
tmp/


# Helm
debug.yml
.cache
.config/helm

#k8s
.kube/

# ikb
.python-version
bin/start_odoo

# Server-specific ignores
test_logs_and_coverage/*
!test_logs_and_coverage/keepme.txt

# Local environment configuration
.claude.local.md
*.local.md

docs/specs/
specs/
specs

node_modules/

inouk-sunray-worker-cloudflare
inouk-sunray-worker-cloudflare/

sunray-srvr*.log

# Database dumps written by `make pg_backup`. They are large and carry live credentials,
# so they live outside the tree that gets committed; the bare *.pg_dump stays because
# dumps used to land in the repo root and older checkouts may still have some.
scrap_zone/
*.pg_dump

# Per-box Claude permissions (`make setup-agent-permissions`). Deliberately NOT committed:
# a committed grant would apply to every clone, production included.
.claude/settings.local.json

